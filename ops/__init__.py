"""Operational helpers shared by the scripts, the chaos runner and the Actor
function: moving aliases, recording each move, and the rules for a safe
rollback. One copy, so the Actor cannot drift from what rollback.py does."""
