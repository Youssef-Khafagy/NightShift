# Local tooling only. Never shipped to Lambda.
#
# The Lambda runtime dependencies live in lambda-deps.in / lambda-deps.lock and
# are cross-built for aarch64. These are the x86 builds the laptop needs to run
# the migration runner and tests against the real cluster.
psycopg[binary]==3.3.6

# The [crt] extra is needed to read the credentials that `aws login` writes.
# The AWS CLI bundles it; plain boto3 does not, and without it every call
# fails with MissingDependencyException. Lambda does not need it, because a
# function gets credentials from its execution role, not from a login cache.
boto3
botocore[crt]

pytest

# The unit tests import the handlers, which import Powertools at module scope.
# Pinned to the same version the Lambda layer ships, so a test never passes
# against a library the deployed code does not have.
aws-lambda-powertools==3.35.0

# moto fakes SQS in process, so the tests exercise the real boto3 call path
# (message attributes included) without an AWS account or a network. Pinned
# because an unpinned test double that changes behaviour breaks CI for
# reasons that have nothing to do with the change under review.
moto[sqs]==5.2.3
