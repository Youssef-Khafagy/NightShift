# Copy to .env (gitignored) and fill in. Never commit .env.
# AWS: no keys here. Credentials come from `aws login --profile nightshift-admin`.
AWS_PROFILE=nightshift-admin
AWS_REGION=ca-central-1

# LLM providers (free tiers), used from M5
GEMINI_API_KEY=
GROQ_API_KEY=
